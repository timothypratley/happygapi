{:TaskLists {:id "TaskLists",
             :type "object",
             :properties {:etag {:type "string",
                                 :description "ETag of the resource."},
                          :nextPageToken {:description "Token that can be used to request the next page of this result.",
                                          :type "string"},
                          :kind {:type "string",
                                 :description "Type of the resource. This is always \"tasks#taskLists\"."},
                          :items {:items {:$ref "TaskList"},
                                  :type "array",
                                  :description "Collection of task lists."}}},
 :Task {:id "Task",
        :properties {:deleted {:type "boolean",
                               :description "Flag indicating whether the task has been deleted. The default is False."},
                     :parent {:type "string",
                              :description "Parent task identifier. This field is omitted if it is a top-level task. This field is read-only. Use the \"move\" method to move the task under a different parent or to the top level."},
                     :updated {:description "Last modification time of the task (as a RFC 3339 timestamp).",
                               :type "string"},
                     :completed {:type "string",
                                 :description "Completion date of the task (as a RFC 3339 timestamp). This field is omitted if the task has not been completed."},
                     :selfLink {:description "URL pointing to this task. Used to retrieve, update, or delete this task.",
                                :type "string"},
                     :etag {:type "string",
                            :description "ETag of the resource."},
                     :due {:type "string",
                           :description "Due date of the task (as a RFC 3339 timestamp). Optional. The due date only records date information; the time portion of the timestamp is discarded when setting the due date. It isn't possible to read or write the time that a task is due via the API."},
                     :title {:description "Title of the task.",
                             :type "string"},
                     :status {:description "Status of the task. This is either \"needsAction\" or \"completed\".",
                              :type "string"},
                     :hidden {:description "Flag indicating whether the task is hidden. This is the case if the task had been marked completed when the task list was last cleared. The default is False. This field is read-only.",
                              :type "boolean"},
                     :id {:type "string",
                          :description "Task identifier."},
                     :kind {:description "Type of the resource. This is always \"tasks#task\".",
                            :type "string"},
                     :notes {:type "string",
                             :description "Notes describing the task. Optional."},
                     :position {:description "String indicating the position of the task among its sibling tasks under the same parent task or at the top level. If this string is greater than another task's corresponding position string according to lexicographical ordering, the task is positioned after the other task under the same parent task (or at the top level). This field is read-only. Use the \"move\" method to move the task to another position.",
                                :type "string"},
                     :links {:items {:properties {:description {:type "string",
                                                                :description "The description. In HTML speak: Everything between <a> and </a>."},
                                                  :type {:description "Type of the link, e.g. \"email\".",
                                                         :type "string"},
                                                  :link {:type "string",
                                                         :description "The URL."}},
                                     :type "object"},
                             :type "array",
                             :description "Collection of links. This collection is read-only."}},
        :type "object"},
 :TaskList {:id "TaskList",
            :type "object",
            :properties {:etag {:description "ETag of the resource.",
                                :type "string"},
                         :title {:description "Title of the task list.",
                                 :type "string"},
                         :kind {:type "string",
                                :description "Type of the resource. This is always \"tasks#taskList\"."},
                         :id {:description "Task list identifier.",
                              :type "string"},
                         :updated {:description "Last modification time of the task list (as a RFC 3339 timestamp).",
                                   :type "string"},
                         :selfLink {:description "URL pointing to this task list. Used to retrieve, update, or delete this task list.",
                                    :type "string"}}},
 :Tasks {:properties {:kind {:type "string",
                             :description "Type of the resource. This is always \"tasks#tasks\"."},
                      :etag {:type "string",
                             :description "ETag of the resource."},
                      :nextPageToken {:description "Token used to access the next page of this result.",
                                      :type "string"},
                      :items {:items {:$ref "Task"},
                              :description "Collection of tasks.",
                              :type "array"}},
         :type "object",
         :id "Tasks"}}
