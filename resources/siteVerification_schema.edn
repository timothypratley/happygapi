{:SiteVerificationWebResourceResource {:type "object",
                                       :id "SiteVerificationWebResourceResource",
                                       :properties {:owners {:description "The email addresses of all verified owners.",
                                                             :items {:type "string"},
                                                             :type "array"},
                                                    :id {:description "The string used to identify this site. This value should be used in the \"id\" portion of the REST URL for the Get, Update, and Delete operations.",
                                                         :type "string"},
                                                    :site {:type "object",
                                                           :description "The address and type of a site that is verified or will be verified.",
                                                           :properties {:type {:description "The site type. Can be SITE or INET_DOMAIN (domain name).",
                                                                               :type "string"},
                                                                        :identifier {:type "string",
                                                                                     :description "The site identifier. If the type is set to SITE, the identifier is a URL. If the type is set to INET_DOMAIN, the site identifier is a domain name."}}}}},
 :SiteVerificationWebResourceGettokenRequest {:type "object",
                                              :properties {:verificationMethod {:description "The verification method that will be used to verify this site. For sites, 'FILE' or 'META' methods may be used. For domains, only 'DNS' may be used.",
                                                                                :type "string"},
                                                           :site {:type "object",
                                                                  :properties {:type {:description "The type of resource to be verified. Can be SITE or INET_DOMAIN (domain name).",
                                                                                      :type "string"},
                                                                               :identifier {:description "The site identifier. If the type is set to SITE, the identifier is a URL. If the type is set to INET_DOMAIN, the site identifier is a domain name.",
                                                                                            :type "string"}},
                                                                  :description "The site for which a verification token will be generated."}},
                                              :id "SiteVerificationWebResourceGettokenRequest"},
 :SiteVerificationWebResourceListResponse {:properties {:items {:type "array",
                                                                :items {:$ref "SiteVerificationWebResourceResource"},
                                                                :description "The list of sites that are owned by the authenticated user."}},
                                           :type "object",
                                           :id "SiteVerificationWebResourceListResponse"},
 :SiteVerificationWebResourceGettokenResponse {:properties {:method {:description "The verification method to use in conjunction with this token. For FILE, the token should be placed in the top-level directory of the site, stored inside a file of the same name. For META, the token should be placed in the HEAD tag of the default page that is loaded for the site. For DNS, the token should be placed in a TXT record of the domain.",
                                                                     :type "string"},
                                                            :token {:type "string",
                                                                    :description "The verification token. The token must be placed appropriately in order for verification to succeed."}},
                                               :type "object",
                                               :id "SiteVerificationWebResourceGettokenResponse"}}
