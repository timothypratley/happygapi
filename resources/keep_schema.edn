{:Group {:id "Group",
         :properties {:email {:description "The group email.",
                              :type "string"}},
         :type "object",
         :description "Describes a single Group."},
 :BatchCreatePermissionsRequest {:type "object",
                                 :description "The request to add one or more permissions on the note. Currently, only the `WRITER` role may be specified. If adding a permission fails, then the entire request fails and no changes are made.",
                                 :id "BatchCreatePermissionsRequest",
                                 :properties {:requests {:items {:$ref "CreatePermissionRequest"},
                                                         :description "The request message specifying the resources to create.",
                                                         :type "array"}}},
 :ListItem {:type "object",
            :description "A single list item in a note's list.",
            :id "ListItem",
            :properties {:childListItems {:description "If set, list of list items nested under this list item. Only one level of nesting is allowed.",
                                          :type "array",
                                          :items {:$ref "ListItem"}},
                         :text {:$ref "TextContent",
                                :description "The text of this item. Length must be less than 1,000 characters."},
                         :checked {:type "boolean",
                                   :description "Whether this item has been checked off or not."}}},
 :Permission {:properties {:email {:description "The email associated with the member. If set on create, the email field in the User or Group message must either be empty or match this field. On read, may be unset if the member does not have an associated email.",
                                   :type "string"},
                           :group {:$ref "Group",
                                   :readOnly true,
                                   :description "Output only. The group to which this role applies."},
                           :role {:description "The role granted by this permission. The role determines the entity’s ability to read, write, and share notes.",
                                  :enumDescriptions ["An undefined role."
                                                     "A role granting full access. This role cannot be added or removed. Defined by the creator of the note."
                                                     "A role granting the ability to contribute content and modify note permissions."],
                                  :enum ["ROLE_UNSPECIFIED"
                                         "OWNER"
                                         "WRITER"],
                                  :type "string"},
                           :deleted {:description "Output only. Whether this member has been deleted. If the member is recovered, this value is set to false and the recovered member retains the role on the note.",
                                     :type "boolean",
                                     :readOnly true},
                           :name {:readOnly true,
                                  :type "string",
                                  :description "Output only. The resource name."},
                           :family {:description "Output only. The Google Family to which this role applies.",
                                    :$ref "Family",
                                    :readOnly true},
                           :user {:description "Output only. The user to whom this role applies.",
                                  :readOnly true,
                                  :$ref "User"}},
              :description "A single permission on the note. Associates a `member` with a `role`.",
              :type "object",
              :id "Permission"},
 :BatchDeletePermissionsRequest {:description "The request to remove one or more permissions from a note. A permission with the `OWNER` role can't be removed. If removing a permission fails, then the entire request fails and no changes are made. Returns a 400 bad request error if a specified permission does not exist on the note.",
                                 :id "BatchDeletePermissionsRequest",
                                 :properties {:names {:description "Required. The names of the permissions to delete. Format: `notes/{note}/permissions/{permission}`",
                                                      :type "array",
                                                      :items {:type "string"}}},
                                 :type "object"},
 :Note {:properties {:permissions {:readOnly true,
                                   :items {:$ref "Permission"},
                                   :type "array",
                                   :description "Output only. The list of permissions set on the note. Contains at least one entry for the note owner."},
                     :trashTime {:type "string",
                                 :readOnly true,
                                 :format "google-datetime",
                                 :description "Output only. If this note has been trashed, when that happened. If trashed, the note will eventually be deleted. If the note is not trashed, this is not set (and the trashed field is false)."},
                     :name {:readOnly true,
                            :type "string",
                            :description "Output only. The resource name of this note. See general note on identifiers in KeepService."},
                     :attachments {:type "array",
                                   :items {:$ref "Attachment"},
                                   :description "Output only. The attachments attached to this note.",
                                   :readOnly true},
                     :createTime {:description "Output only. When this note was created.",
                                  :type "string",
                                  :readOnly true,
                                  :format "google-datetime"},
                     :title {:type "string",
                             :description "The title of the note. Length must be less than 1,000 characters."},
                     :updateTime {:readOnly true,
                                  :type "string",
                                  :description "Output only. When this note was last modified.",
                                  :format "google-datetime"},
                     :trashed {:type "boolean",
                               :description "Output only. True if this note has been trashed. If trashed, the note will eventually be deleted.",
                               :readOnly true},
                     :body {:$ref "Section",
                            :description "The body of the note."}},
        :description "A single note.",
        :id "Note",
        :type "object"},
 :ListNotesResponse {:type "object",
                     :properties {:notes {:type "array",
                                          :description "A page of notes.",
                                          :items {:$ref "Note"}},
                                  :nextPageToken {:description "Next page's `page_token` field.",
                                                  :type "string"}},
                     :id "ListNotesResponse",
                     :description "The response when listing a page of notes (see KeepService documentation)."},
 :Empty {:type "object",
         :id "Empty",
         :description "A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); } The JSON representation for `Empty` is empty JSON object `{}`.",
         :properties {}},
 :User {:properties {:email {:type "string",
                             :description "The user's email."}},
        :id "User",
        :description "Describes a single user.",
        :type "object"},
 :Attachment {:id "Attachment",
              :description "An attachment to a note.",
              :type "object",
              :properties {:mimeType {:description "The MIME types (IANA media types) in which the attachment is available.",
                                      :type "array",
                                      :items {:type "string"}},
                           :name {:type "string",
                                  :description "The resource name;"}}},
 :Section {:id "Section",
           :type "object",
           :description "The content of the note.",
           :properties {:list {:description "Used if this section's content is a list.",
                               :$ref "ListContent"},
                        :text {:$ref "TextContent",
                               :description "Used if this section's content is a block of text. The length of the text content must be less than 20,000 characters."}}},
 :TextContent {:description "The block of text for a single text section or list item.",
               :id "TextContent",
               :type "object",
               :properties {:text {:description "The text of the note. The limits on this vary with the specific field using this type.",
                                   :type "string"}}},
 :Family {:type "object",
          :description "Describes a single Google Family.",
          :properties {},
          :id "Family"},
 :BatchCreatePermissionsResponse {:type "object",
                                  :properties {:permissions {:description "Permissions created.",
                                                             :type "array",
                                                             :items {:$ref "Permission"}}},
                                  :id "BatchCreatePermissionsResponse",
                                  :description "The response for creating permissions on a note."},
 :ListContent {:properties {:listItems {:description "The items in the list. The number of items must be less than 1,000.",
                                        :type "array",
                                        :items {:$ref "ListItem"}}},
               :id "ListContent",
               :description "The list of items for a single list note.",
               :type "object"},
 :CreatePermissionRequest {:id "CreatePermissionRequest",
                           :description "The request to add a single permission on the note.",
                           :properties {:parent {:description "Required. The parent note where this permission will be created. Format: `notes/{note}`",
                                                 :type "string"},
                                        :permission {:description "Required. The permission to create. One of Permission.email, User.email or Group.email must be supplied.",
                                                     :$ref "Permission"}},
                           :type "object"}}
