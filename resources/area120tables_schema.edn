{:RelationshipDetails {:properties {:linkedTable {:type "string",
                                                  :description "The name of the table this relationship is linked to."}},
                       :description "Details about a relationship column.",
                       :id "RelationshipDetails",
                       :type "object"},
 :Workspace {:id "Workspace",
             :properties {:displayName {:type "string",
                                        :description "The human readable title of the workspace."},
                          :updateTime {:type "string",
                                       :description "Time when the workspace was last updated.",
                                       :format "google-datetime"},
                          :tables {:description "The list of tables in the workspace.",
                                   :type "array",
                                   :items {:$ref "Table"}},
                          :createTime {:type "string",
                                       :format "google-datetime",
                                       :description "Time when the workspace was created."},
                          :name {:type "string",
                                 :description "The resource name of the workspace. Workspace names have the form `workspaces/{workspace}`."}},
             :description "A single workspace.",
             :type "object"},
 :Table {:properties {:savedViews {:type "array",
                                   :description "Saved views for this table.",
                                   :items {:$ref "SavedView"}},
                      :displayName {:description "The human readable title of the table.",
                                    :type "string"},
                      :columns {:type "array",
                                :description "List of columns in this table. Order of columns matches the display order.",
                                :items {:$ref "ColumnDescription"}},
                      :name {:description "The resource name of the table. Table names have the form `tables/{table}`.",
                             :type "string"},
                      :updateTime {:description "Time when the table was last updated excluding updates to individual rows",
                                   :type "string",
                                   :format "google-datetime"},
                      :createTime {:type "string",
                                   :description "Time when the table was created.",
                                   :format "google-datetime"}},
         :description "A single table. NextId: 7",
         :type "object",
         :id "Table"},
 :CreateRowRequest {:properties {:row {:description "Required. The row to create.",
                                       :$ref "Row"},
                                 :view {:enumDescriptions ["Defaults to user entered text."
                                                           "Uses internally generated column id to identify values."],
                                        :type "string",
                                        :enum ["VIEW_UNSPECIFIED"
                                               "COLUMN_ID_VIEW"],
                                        :description "Optional. Column key to use for values in the row. Defaults to user entered name."},
                                 :parent {:description "Required. The parent table where this row will be created. Format: tables/{table}",
                                          :type "string"}},
                    :type "object",
                    :description "Request message for TablesService.CreateRow.",
                    :id "CreateRowRequest"},
 :ColumnDescription {:type "object",
                     :description "Details on a column in the table.",
                     :properties {:labels {:type "array",
                                           :items {:$ref "LabeledItem"},
                                           :description "Optional. Range of labeled values for the column. Some columns like tags and drop-downs limit the values to a set of possible values. We return the range of values in such cases to help clients implement better user data validation."},
                                  :dataType {:type "string",
                                             :description "Data type of the column Supported types are auto_id, boolean, boolean_list, creator, create_timestamp, date, dropdown, location, integer, integer_list, number, number_list, person, person_list, tags, check_list, text, text_list, update_timestamp, updater, relationship, file_attachment_list. These types directly map to the column types supported on Tables website."},
                                  :readonly {:description "Optional. Indicates that values for the column cannot be set by the user.",
                                             :type "boolean"},
                                  :lookupDetails {:description "Optional. Indicates that this is a lookup column whose value is derived from the relationship column specified in the details. Lookup columns can not be updated directly. To change the value you must update the associated relationship column.",
                                                  :$ref "LookupDetails"},
                                  :relationshipDetails {:description "Optional. Additional details about a relationship column. Specified when data_type is relationship.",
                                                        :$ref "RelationshipDetails"},
                                  :id {:type "string",
                                       :description "Internal id for a column."},
                                  :multipleValuesDisallowed {:description "Optional. Indicates whether or not multiple values are allowed for array types where such a restriction is possible.",
                                                             :type "boolean"},
                                  :name {:type "string",
                                         :description "column name"}},
                     :id "ColumnDescription"},
 :ListTablesResponse {:type "object",
                      :properties {:tables {:items {:$ref "Table"},
                                            :description "The list of tables.",
                                            :type "array"},
                                   :nextPageToken {:description "A token, which can be sent as `page_token` to retrieve the next page. If this field is empty, there are no subsequent pages.",
                                                   :type "string"}},
                      :description "Response message for TablesService.ListTables.",
                      :id "ListTablesResponse"},
 :ListWorkspacesResponse {:type "object",
                          :description "Response message for TablesService.ListWorkspaces.",
                          :properties {:workspaces {:items {:$ref "Workspace"},
                                                    :type "array",
                                                    :description "The list of workspaces."},
                                       :nextPageToken {:description "A token, which can be sent as `page_token` to retrieve the next page. If this field is empty, there are no subsequent pages.",
                                                       :type "string"}},
                          :id "ListWorkspacesResponse"},
 :BatchDeleteRowsRequest {:id "BatchDeleteRowsRequest",
                          :description "Request message for TablesService.BatchDeleteRows",
                          :type "object",
                          :properties {:names {:items {:type "string"},
                                               :type "array",
                                               :description "Required. The names of the rows to delete. All rows must belong to the parent table or else the entire batch will fail. A maximum of 500 rows can be deleted in a batch. Format: tables/{table}/rows/{row}"}}},
 :Row {:description "A single row in a table.",
       :properties {:createTime {:type "string",
                                 :description "Time when the row was created.",
                                 :format "google-datetime"},
                    :values {:description "The values of the row. This is a map of column key to value. Key is user entered name(default) or the internal column id based on the view in the request.",
                             :type "object",
                             :additionalProperties {:type "any"}},
                    :name {:type "string",
                           :description "The resource name of the row. Row names have the form `tables/{table}/rows/{row}`. The name is ignored when creating a row."},
                    :updateTime {:description "Time when the row was last updated.",
                                 :format "google-datetime",
                                 :type "string"}},
       :id "Row",
       :type "object"},
 :BatchUpdateRowsResponse {:type "object",
                           :properties {:rows {:type "array",
                                               :description "The updated rows.",
                                               :items {:$ref "Row"}}},
                           :id "BatchUpdateRowsResponse",
                           :description "Response message for TablesService.BatchUpdateRows."},
 :Empty {:description "A generic empty message that you can re-use to avoid defining duplicated empty messages in your APIs. A typical example is to use it as the request or the response type of an API method. For instance: service Foo { rpc Bar(google.protobuf.Empty) returns (google.protobuf.Empty); } The JSON representation for `Empty` is empty JSON object `{}`.",
         :id "Empty",
         :properties {},
         :type "object"},
 :BatchCreateRowsRequest {:type "object",
                          :description "Request message for TablesService.BatchCreateRows.",
                          :id "BatchCreateRowsRequest",
                          :properties {:requests {:description "Required. The request message specifying the rows to create. A maximum of 500 rows can be created in a single batch.",
                                                  :items {:$ref "CreateRowRequest"},
                                                  :type "array"}}},
 :ListRowsResponse {:id "ListRowsResponse",
                    :type "object",
                    :properties {:nextPageToken {:type "string",
                                                 :description "A token, which can be sent as `page_token` to retrieve the next page. If this field is empty, there are no subsequent pages."},
                                 :rows {:description "The rows from the specified table.",
                                        :type "array",
                                        :items {:$ref "Row"}}},
                    :description "Response message for TablesService.ListRows."},
 :LabeledItem {:id "LabeledItem",
               :type "object",
               :properties {:id {:description "Internal id associated with the item.",
                                 :type "string"},
                            :name {:description "Display string as entered by user.",
                                   :type "string"}},
               :description "A single item in a labeled column."},
 :BatchCreateRowsResponse {:description "Response message for TablesService.BatchCreateRows.",
                           :properties {:rows {:description "The created rows.",
                                               :type "array",
                                               :items {:$ref "Row"}}},
                           :id "BatchCreateRowsResponse",
                           :type "object"},
 :BatchUpdateRowsRequest {:id "BatchUpdateRowsRequest",
                          :description "Request message for TablesService.BatchUpdateRows.",
                          :type "object",
                          :properties {:requests {:description "Required. The request messages specifying the rows to update. A maximum of 500 rows can be modified in a single batch.",
                                                  :items {:$ref "UpdateRowRequest"},
                                                  :type "array"}}},
 :SavedView {:properties {:id {:type "string",
                               :description "Internal id associated with the saved view."},
                          :name {:description "Display name of the saved view.",
                                 :type "string"}},
             :id "SavedView",
             :description "A saved view of a table. NextId: 3",
             :type "object"},
 :UpdateRowRequest {:description "Request message for TablesService.UpdateRow.",
                    :type "object",
                    :id "UpdateRowRequest",
                    :properties {:updateMask {:type "string",
                                              :format "google-fieldmask",
                                              :description "The list of fields to update."},
                                 :view {:enum ["VIEW_UNSPECIFIED"
                                               "COLUMN_ID_VIEW"],
                                        :enumDescriptions ["Defaults to user entered text."
                                                           "Uses internally generated column id to identify values."],
                                        :type "string",
                                        :description "Optional. Column key to use for values in the row. Defaults to user entered name."},
                                 :row {:$ref "Row",
                                       :description "Required. The row to update."}}},
 :LookupDetails {:id "LookupDetails",
                 :type "object",
                 :properties {:relationshipColumn {:type "string",
                                                   :description "The name of the relationship column associated with the lookup."},
                              :relationshipColumnId {:description "The id of the relationship column.",
                                                     :type "string"}},
                 :description "Details about a lookup column whose value comes from the associated relationship."}}
